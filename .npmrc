# pnpm's workspace-root install safeguard otherwise blocks CI tools that
# install themselves at the root (e.g. cloudflare/wrangler-action installing
# wrangler before deploying). The safeguard is informational, not security.
ignore-workspace-root-check=true
