---
title: Quick start
description: Index your repo and ask your first question in under five minutes.
---

import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

<Aside type="tip" title="Just want to try it?">
Skip install entirely — open the [in-browser playground](/playground/). It runs against a pre-indexed `zustand` repo using sql.js. No keys required to browse Risk / History / Related; bring your own Gemini key only if you want to use Ask.
</Aside>

## Install + index

```sh
# 1. Initialize gitwhy in your repo
npx @kamsqe/gitwhy init

# 2. Set your LLM credentials (one of these)
export OPENAI_API_KEY=sk-...
export GEMINI_API_KEY=AIza...

# 3. Estimate cost, then index
npx @kamsqe/gitwhy estimate
npx @kamsqe/gitwhy index

# 4. Ask anything
npx @kamsqe/gitwhy why "why does processPayment have a 30 second timeout?"
```

After `npm install -g @kamsqe/gitwhy` the binary is just `gitwhy` — every `npx @kamsqe/gitwhy …` invocation below also works as bare `gitwhy …`.

## What just happened

1. **`init`** created a `.gitwhy/` directory with a SQLite database, a config file, and a `traces/` directory for optional NDJSON observability.
2. **`estimate`** walked your repo's git history, categorized each commit (merge / bot / formatting / size-based), and projected how many LLM calls indexing would make plus the rough cost. No network was hit.
3. **`index`** did the real work: for each commit, it ran the categorizer, optionally clustered consecutive micro-commits, decomposed mega-commits, scrubbed secrets from diffs, called the LLM for an enriched summary, generated a vector embedding, and stored everything in the SQLite database. The whole thing is resumable — interrupt it and re-run `index` to continue.
4. **`why`** embedded your question, cosine-searched the stored embeddings, loaded the top-K commits, and synthesized a citation-backed answer with confidence gating.

<Aside type="note" title="Incremental by default">
`gitwhy index` resumes from the last indexed commit. Re-running after new commits land only processes the delta. Pass `--full` to reindex from scratch (useful after upgrading the schema or pulling in `.gitwhyignore` changes).
</Aside>

## More CLI commands

```sh
gitwhy status                                # Coverage + token spend + hotspots
gitwhy risk <path>                           # Risk assessment for a file
gitwhy related <path>                        # Co-changing files
gitwhy commit [--apply]                      # Auto-generate a commit message
gitwhy catchup --since "1 week ago"          # Recent activity digest
gitwhy mcp-doctor                            # Diagnose MCP setup
gitwhy feedback up|down -q "..."             # Record answer feedback
gitwhy mcp                                   # Start the MCP server on stdio
```

## Three ways to use it

<CardGrid>
  <LinkCard
    title="MCP — the headline feature"
    description="Wire GitWhy into Cursor, Claude Code, or Windsurf. Your editor's agent calls gitwhy.why on its own."
    href="/mcp-setup/"
  />
  <LinkCard
    title="Web UI"
    description="Run gitwhy serve locally, then drive Ask / Risk / Related / History through a browser."
    href="/app/"
  />
  <LinkCard
    title="Pure CLI"
    description="Everything works headless. Useful in CI, scripts, and the GitHub Action."
    href="https://github.com/kamsqe/gitwhy#readme"
  />
</CardGrid>

## Working offline

Set `GITWHY_USE_MOCK_LLM=1` to use the deterministic mock provider — useful for testing the pipeline without burning tokens. A first-class Ollama provider is on the post-launch roadmap; the `LlmProvider` interface already accommodates it.

## Cost transparency

GitWhy never indexes without telling you the projected cost first. The `estimate` command dry-runs the entire pipeline and reports per-category projected tokens + USD before any cloud call is made.

Indexing 5k commits on a real repo with `gpt-4o-mini` typically costs **$0.10–$0.50** depending on commit size distribution. With `gemini-2.5-flash` on the free tier it costs **$0** but takes longer due to RPM pacing.

<Aside type="caution" title="Secrets are pre-scrubbed, not stored">
Diffs run through a 12-pattern secret scanner (AWS, GitHub, OpenAI, Anthropic, Slack, Stripe, JWT, PEM, generic `KEY=value`) before they leave your machine. Detected secrets are redacted before the LLM sees them. Run `gitwhy diagnostics` after indexing to confirm the scanner ran cleanly.
</Aside>
